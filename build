#!/bin/bash

image=getup-engine:${1:-v3.9}
dockerfile=Dockerfile-${1:-v3.9}

echo "---> Building $image from $dockerfile"

[ "${NO_PULL}" != 1 ] && git pull
sudo docker build . -t $image -f $dockerfile
