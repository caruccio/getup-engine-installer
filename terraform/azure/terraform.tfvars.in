cluster_zone    = "${CLUSTER_ZONE}"
apps_zone       = "${APPS_ZONE}"

master_count = ${MASTER_COUNT}
infra_count  = ${INFRA_COUNT}
app_count    = ${APP_COUNT}

azure_instance_bastion  = "${AZURE_INSTANCE_BASTION}"
azure_instance_master   = "${AZURE_INSTANCE_MASTER}"
azure_instance_infra    = "${AZURE_INSTANCE_INFRA}"
azure_instance_app      = "${AZURE_INSTANCE_APP}"
azure_database_capacity = "${AZURE_DATABASE_CAPACITY}"
azure_database_tier     = "${AZURE_DATABASE_TIER}"

azure_subscription_id = "${AZURE_SUBSCRIPTION_ID}"
azure_location        = "${AZURE_LOCATION}"
azure_resource_group  = "${AZURE_RESOURCE_GROUP}"
azure_user_public_key = "${ID_RSA_PUB}"
